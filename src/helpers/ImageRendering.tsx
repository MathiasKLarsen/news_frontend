const imageArticleURL = `http://localhost:3001/assets/images/${imageContent.url}`;
const imageVideoURL = `http://localhost:3001/assets/video/${imageContent.url}`;
const imagePodcastURL = `http://localhost:3001/assets/podcast/${imageContent.url}`;